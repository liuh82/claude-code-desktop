# Task: 对齐 Stitch 设计稿 — Chat Area 完整精修

## 背景
蓝色主题已切换完成（commit 93c480e）。现在需要对比设计稿 HTML，把所有剩余差异全部修掉。

## 设计稿位置
- **Light**: `/tmp/stitch-new/stitch_main_chat_interface/main_chat_full_light_orange_accent_v2/code.html`
- 读取这个文件获取精确 token，不要猜任何数值。

## 绝对规则
1. **只改 CSS property 值，禁止改 class selector 名**
2. **禁止 inline style**
3. **禁止 `width: 100%` 在 flex 子元素上**
4. **JS ↔ CSS 同步** — auto-resize 里的最小高度要和 CSS `min-height` 一致
5. **每个数值都从设计稿 HTML 提取，不凭感觉**

## 需要检查和修复的组件

### 1. 消息布局和间距
设计稿：
```html
<div class="max-w-3xl mx-auto space-y-10">  <!-- max-w-3xl = 48rem = 768px, space-y-10 = 40px -->
```
- 消息区域最大宽度 768px，居中 ✅（已有）
- 消息间距 40px（space-y-10）→ 检查当前是否匹配
- 消息区域 padding `p-8`（32px）→ 检查当前

### 2. AI 消息
设计稿：
```html
<div class="flex gap-6 group">
  <div class="w-8 h-8 rounded-lg bg-gradient-to-br from-primary to-blue-600 flex-shrink-0 flex items-center justify-center mt-1 shadow-md shadow-primary/20">
    <span class="material-symbols-outlined text-on-primary text-sm" data-weight="fill">smart_toy</span>
  </div>
  <div class="flex-1 space-y-4">
    <div class="text-on-surface leading-relaxed text-[15px]">
```
关键点：
- `gap-6`（24px）间距
- 头像 `w-8 h-8`（32px），`rounded-lg`（8px），`mt-1`（4px）
- 渐变 `from-primary(#005bc0) to-blue-600(#2563eb)`
- 阴影 `shadow-md shadow-primary/20`
- 图标 `text-sm`（14px），`data-weight="fill"`（FILL 1）
- 正文 `text-[15px]`，`leading-relaxed`
- inline code: `bg-primary/5 text-primary border border-primary/20 px-1.5 py-0.5 rounded font-mono text-sm font-semibold`

### 3. 用户消息
设计稿：
```html
<div class="flex gap-6 justify-end">
  <div class="max-w-[80%] bg-indigo-50 border border-indigo-100 px-6 py-4 rounded-3xl rounded-tr-sm shadow-sm">
    <p class="text-[14px] text-indigo-900 font-medium">请展示相关的代码差异...</p>
  </div>
  <div class="w-8 h-8 rounded-lg bg-indigo-600 flex-shrink-0 flex items-center justify-center mt-1 shadow-md shadow-indigo-200">
    <span class="material-symbols-outlined text-on-secondary text-sm" data-weight="fill">person</span>
  </div>
</div>
```
关键点：
- `gap-6`（24px）
- `justify-end` 右对齐 ✅
- 气泡 `max-w-[80%]`，`bg-indigo-50`，`border-indigo-100`，`px-6 py-4`（24px 16px），`rounded-3xl rounded-tr-sm`（24px 24px 24px 4px）
- 气泡文字 `text-[14px] text-indigo-900 font-medium`
- 用户头像 `bg-indigo-600`，`shadow-md shadow-indigo-200`

### 4. 代码块
设计稿：
```html
<div class="rounded-xl border border-outline-variant/40 bg-white overflow-hidden shadow-sm">
  <div class="flex items-center justify-between px-4 py-2.5 bg-surface-container/50 border-b border-outline-variant/10">
    <div class="flex items-center gap-2">
      <span class="w-3 h-3 rounded-full bg-red-400"></span>
      <span class="w-3 h-3 rounded-full bg-amber-400"></span>
      <span class="w-3 h-3 rounded-full bg-emerald-400"></span>
      <span class="ml-2 text-[11px] font-bold font-mono text-primary uppercase tracking-widest">TypeScript</span>
    </div>
    <span class="material-symbols-outlined text-[16px] text-outline cursor-pointer hover:text-primary transition-colors">content_copy</span>
  </div>
  <pre class="p-5 text-[13px] font-mono leading-relaxed overflow-x-auto text-on-surface-variant">
```
关键点：
- `rounded-xl`（12px）
- `border-outline-variant/40`，`bg-white`
- Header: `px-4 py-2.5`，`bg-surface-container/50`
- Traffic lights: 红/黄/绿圆点 `w-3 h-3 rounded-full`，`bg-red-400 bg-amber-400 bg-emerald-400`
- 语言标签: `text-[11px] font-bold font-mono text-primary uppercase tracking-widest`
- 复制图标: `text-[16px]`
- 代码区: `p-5`，`text-[13px] font-mono leading-relaxed`
- 语法高亮: keyword `#005bc0`, string `#0891b2`, comment `#94a3b8 italic`

### 5. 工具调用卡片
设计稿：
```html
<div class="flex items-center gap-3 p-3.5 rounded-xl border border-primary/30 bg-primary/5 group/tool cursor-pointer hover:bg-primary/10 transition-all shadow-sm">
  <div class="w-7 h-7 rounded-lg bg-primary/20 flex items-center justify-center">
    <span class="material-symbols-outlined text-primary text-[18px]">terminal</span>
  </div>
  <span class="text-[13px] text-primary font-bold flex-1">运行测试: npm run test:engine</span>
  <span class="material-symbols-outlined text-primary text-sm group-hover/tool:translate-x-0.5 transition-transform">chevron_right</span>
</div>
```
关键点：
- `gap-3`（12px），`p-3.5`（14px），`rounded-xl`（12px）
- `border-primary/30`，`bg-primary/5`
- 图标容器: `w-7 h-7 rounded-lg bg-primary/20`
- 图标: `text-[18px]`
- 标题: `text-[13px] text-primary font-bold`
- 右侧箭头: `chevron_right`

### 6. 输入区域
设计稿：
```html
<div class="p-6 bg-white/60 backdrop-blur-xl border-t border-outline-variant/20">
  <div class="max-w-3xl mx-auto relative group">
    <textarea class="w-full bg-white border-2 border-outline-variant/20 rounded-2xl px-5 py-5 pr-14 text-[14px] focus:ring-4 focus:ring-primary/5 focus:border-primary/40 outline-none transition-all resize-none min-h-[64px] hide-scrollbar shadow-sm" placeholder="输入指令，例如 '/patch fix-lock-issue'..." rows="1"></textarea>
    <div class="absolute right-4 bottom-4 flex gap-2">
      <button class="p-2 rounded-xl text-outline hover:text-indigo-600 hover:bg-indigo-50 transition-all">
        <span class="material-symbols-outlined text-[20px]">attach_file</span>
      </button>
      <button class="p-2 rounded-xl text-on-primary shadow-lg hover:brightness-110 hover:-translate-y-0.5 transition-all bg-orange-500 shadow-orange-500/30">
        <span class="material-symbols-outlined text-[20px]" data-weight="fill">arrow_upward</span>
      </button>
    </div>
  </div>
  <!-- Footer: model indicator + shortcuts -->
  <div class="max-w-3xl mx-auto mt-4 flex items-center justify-between">
    <div class="flex gap-5">
      <span class="text-[11px] text-emerald-600 flex items-center gap-2 uppercase font-bold tracking-wider">
        <span class="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></span>Claude 3.5 Sonnet
      </span>
    </div>
    <div class="flex gap-3">
      <span class="text-[11px] text-primary/60 hover:text-primary cursor-pointer transition-colors uppercase font-black tracking-widest bg-primary/5 px-2 py-0.5 rounded">⌘+Enter 发送</span>
    </div>
  </div>
</div>
```
关键点：
- 区域: `p-6`，`bg-white/60 backdrop-blur-xl`，`border-t border-outline-variant/20`
- Textarea: `bg-white`，`border-2 border-outline-variant/20`，`rounded-2xl`，`px-5 py-5 pr-14`，`text-[14px]`
- Focus: `ring-4 ring-primary/5 border-primary/40`
- `min-h-[64px]`
- 发送按钮: `p-2 rounded-xl bg-orange-500 shadow-orange-500/30`（橙色！）
- 附件按钮: `text-outline hover:text-indigo-600 hover:bg-indigo-50`
- **底部状态栏**: 模型名 + 快捷键提示（检查是否存在）

### 7. ChatHeader（顶部栏）
设计稿有一个 TopNav，但我们用 ChatHeader 替代了。检查 ChatHeader 样式：
- 路径 pill 应该是: `bg-primary-container/20 px-3 py-1 rounded-full border border-primary/20`
- 路径文字: `text-[10px] text-primary font-mono font-bold tracking-wider`
- 确保与蓝色主题一致

## 执行步骤
1. 读取设计稿 HTML: `/tmp/stitch-new/stitch_main_chat_interface/main_chat_full_light_orange_accent_v2/code.html`
2. 逐个组件对比当前 CSS module 中的值
3. 列出所有差异（精确到属性和值）
4. 修复所有差异
5. `npx tsc --noEmit` 确保无 TS 错误
6. `npm run build` 确保构建通过

## 不要做的事
- 不要改 TSX 文件结构，只改 CSS 值
- 不要改 class selector 名
- 不要添加 inline style
- 不要猜测数值，一切从设计稿提取
