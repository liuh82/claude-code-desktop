# Task: 严格复刻最终设计稿 — 结构性对齐

## 背景
之前用的设计稿是错误版本。用户刚刚提供了最终版设计稿，位于 `/tmp/stitch-final-v2/`。
当前实现与最终设计稿有**结构性差异**，不只是像素级微调。

## ⚠️ 最终设计稿位置（唯一正确版本）
- **Light**: `/tmp/stitch-final-v2/stitch_main_chat_interface/main_chat_desktop_balanced_blue_orange/code.html`
- **Light 截图**: `/tmp/stitch-final-v2/stitch_main_chat_interface/main_chat_desktop_balanced_blue_orange/screen.png`
- **Dark**: `/tmp/stitch-final-v2/stitch_main_chat_interface/main_chat_desktop_dark_distinct_panels/code.html`
- ❌ 不要读 `/tmp/stitch-new/` 或 `/tmp/stitch-final/`

## 任务：先输出计划，不要直接写代码

请先读取设计稿 HTML，然后输出一个详细的实施计划，列出所有需要修改的组件和文件。

### 计划应包括

#### A. 结构性差异（需要改 TSX + CSS）
逐一分析设计稿中的每个组件，对比当前代码，列出：
1. 侧边栏：设计稿是 64px (w-16)，当前是 48px。设计稿有：
   - 橙色 "C" logo (w-10 h-10 bg-brand-orange rounded-lg)
   - 导航图标（code, search, history, extension）
   - Active 状态: border-l-2 border-blue-600 bg-white
   - 底部: settings 图标 + 用户头像（圆形，brand-orange 边框）
   - 背景: bg-slate-100, border-r border-slate-200
2. ChatHeader: 设计稿有（h-14, px-6），包含：
   - 文件夹 pill (bg-surface-container px-3 py-1 rounded-full)
   - 模型指示器 (Claude 3.7 Sonnet + model_training 图标)
   - more_horiz 菜单按钮
3. 消息布局：设计稿中
   - AI 消息：左对齐，头像 bg-brand-orange（不是蓝渐变！），有名字 "Claude Code"
   - 用户消息：也是左对齐！用户头像在左，圆形 (rounded-full) brand-orange/20 边框
   - 消息间距: space-y-8 (32px)，不是 40px
   - 没有气泡！消息是纯文本布局，没有背景色
4. Diff/Action 卡片：设计稿中在消息之间有：
   - 标题栏 (MODIFIED + 文件路径 + 拒绝/应用更改 按钮)
   - Diff 内容区（行号 + 增删改高亮）
5. 输入区：设计稿有重大不同：
   - 外层 bg-surface（不是半透明 backdrop-blur）
   - 输入框 bg-surface-container-lowest border rounded-2xl shadow-sm
   - 内部有 p-3 padding，textarea min-h-[44px]
   - 底部工具栏（attach_file + terminal + 发送按钮）在输入框内，用 border-t 分隔
   - 发送按钮 bg-brand-orange rounded-xl
   - 输入框下方有居中文字 "Claude Code 正在预览阶段 · 使用 ⌘+K 快速唤起命令"
6. 右侧面板 (w-72 = 288px，不是 420px)：
   - 标题 "资源管理器"
   - 文件树（带选中状态 blue-50 + border-r-2 border-blue-600）
   - 底部 "上下文状态" 面板（代码行数、更改文件数、进度条）
   - hidden xl:flex（在大屏才显示）

#### B. 需要更新/删除的组件
- 消息气泡样式（设计稿没有气泡）
- 空状态页面（设计稿没有）
- 用户消息右对齐（设计稿是左对齐！）

#### C. 颜色系统差异
- AI 头像：设计稿用 bg-brand-orange，不是蓝色渐变
- 用户头像：设计稿用圆形 rounded-full + brand-orange/20 边框
- 侧边栏背景：bg-slate-100 border-slate-200（不是 surface-container-low）
- Active 导航：border-l-2 border-blue-600（不是 3px 左边框 + bg primary/10）

## 执行规则
1. **先输出计划，等人类确认后再执行**
2. 读取设计稿 HTML 获取精确 token
3. 只改 CSS property 值和必要的 TSX 结构
4. 不改 class selector 名（除非是新组件）
5. 不加 inline style
6. 每个 commit 都要 tsc + build 通过
