# Task: 执行设计稿对齐计划

你已经输出了详细计划，现在请按计划的 4 个阶段依次执行所有修改。

## 执行规则
1. 按阶段顺序执行：结构骨架 → 消息核心 → 输入区 → 细节打磨
2. 每个阶段完成后跑 `npx tsc --noEmit` 确认无 TS 错误
3. 全部完成后跑 `npm run build` 确认构建通过
4. 只改 CSS property 值和必要的 TSX 结构
5. 不改 class selector 名（除非是新组件）
6. 不加 inline style
7. 每个数值从设计稿 HTML 提取：`/tmp/stitch-final-v2/stitch_main_chat_interface/main_chat_desktop_balanced_blue_orange/code.html`

## 22 个修改项（按你计划中的编号）

### 第一阶段：结构骨架
1. Sidebar 宽度 48→64px
2. ChatHeader 高度 48→56px
3. ToolPanel 宽度 340→288px

### 第二阶段：消息核心
4. MessageBubble 用户消息改左对齐，移除气泡
5. AI 头像改橙色 bg-brand-orange
6. 用户头像改圆形+橙色边框
7. AI 名称改 "Claude Code"
8. 消息间距 40→32px

### 第三阶段：输入区重写
9. InputArea 容器化重构（textarea + 工具栏合并）
10. 移除毛玻璃→实色背景
11. textarea padding 紧凑化，min-height 44px
12. Footer 改居中文字 "Claude Code 正在预览阶段 · 使用 ⌘+K 快速唤起命令"

### 第四阶段：细节打磨
13. Sidebar Logo 改橙色 "C" 字母
14. Sidebar 导航项改为 code/search/history/extension
15. Sidebar Active 状态 border-l-2px + bg-white
16. Sidebar 底部加用户头像，移除 themeToggle
17. ChatHeader path-pill 改灰底
18. ChatHeader model info 移除 uppercase
19. ToolPanel 标题改为"资源管理器"
20. ToolPanel 头部高度 56px
21. FileTree 选中状态样式
22. 发送按钮保持 bg-brand-orange

## 保留不改
- TabBar、空状态、命令面板、设置弹窗、DiffView 组件（不删除）、ResizeHandle

开始执行。
